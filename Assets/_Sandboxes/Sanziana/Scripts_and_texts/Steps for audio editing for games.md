
//if =! sound
    check Input/output
    if =! output
        then control pannel (reaper)
//AI for path settings

 In PROJECT folder activate COPY ALL MEDIA
On edit
    in out of sample needs to be exactly on silence
        On Low Cut
            pasi 50-60 hz
        On High Cut
           pasi 7-8 khz
    Check In/out
        
    Chronological steps

        A. Make test event (check in/out)
        Check w/ engine and sync
         (adjust in if out of syn w/ anim)
         FX
            1_SHAPE.IT (on event, never on track)
            2_CROSS FADE (ambiance)
            3_PLACE IT (on event, never on track)
                if reverb == event, don't cut
        If offline processing (the edits on the cut sample) 
        spills into the whole event


On export
    one export each sample
    export each clip[sample]
    export in wav

